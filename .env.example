# Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=your_password
DATABASE_NAME=readbox_db

# JWT
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=7d

# VNPay Configuration
# Sandbox cho test: https://sandbox.vnpayment.vn/
VNPAY_TMN_CODE=YOUR_TMN_CODE
VNPAY_HASH_SECRET=YOUR_HASH_SECRET
VNPAY_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html

# VNPAY_RETURN_URL: URL callback sau khi thanh toán
# - Development với mobile app:
#   + Android Emulator: http://10.0.2.2:4000/payment/vnpay/callback
#   + iOS Simulator: http://localhost:4000/payment/vnpay/callback
#   + Real Device (cùng WiFi): http://192.168.x.x:4000/payment/vnpay/callback (thay bằng IP máy tính)
# - Production: https://your-domain.com/payment/vnpay/callback
VNPAY_RETURN_URL=http://10.0.2.2:4000/payment/vnpay/callback

# VNPAY_IPN_URL: URL webhook từ VNPay (phải public, không dùng localhost)
VNPAY_IPN_URL=https://your-domain.com/payment/vnpay/ipn

# MoMo Configuration (optional)
# MOMO_PARTNER_CODE=
# MOMO_ACCESS_KEY=
# MOMO_SECRET_KEY=
# MOMO_ENDPOINT=

# App Configuration
APP_PORT=4000
APP_URL=http://localhost:4000

# Gemini AI
GEMINI_API_KEY=your_gemini_api_key_here

# OCR / RabbitMQ
# Credential cho RabbitMQ trong docker-compose (đừng dùng guest vì guest chỉ
# kết nối được từ loopback)
RABBITMQ_DEFAULT_USER=readbox
RABBITMQ_DEFAULT_PASS=readbox123
# Chuỗi kết nối RabbitMQ cho module OCR (backend <-> Python worker)
RABBITMQ_URL=amqp://readbox:readbox123@localhost:5672
# Tên hàng đợi gửi job cho worker
OCR_JOBS_QUEUE=ocr.jobs
# Tên hàng đợi nhận kết quả từ worker
OCR_RESULTS_QUEUE=ocr.results
# Số message worker xử lý đồng thời (prefetch khi consume kết quả)
OCR_PREFETCH=4
